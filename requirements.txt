# Mirrors the deps installed in alpha-oversight/.venv (Python 3.12).
# Pinned to the versions present at Phase-0 scaffold time.
litellm==1.89.0
pydantic==2.13.4
numpy==2.4.6
fastapi==0.136.3
uvicorn==0.49.0
httpx==0.28.1
websockets==16.0
python-dotenv==1.2.2
aiosqlite==0.22.1
tiktoken==0.13.0
structlog==26.1.0
# test-only
pytest==9.1.0
pytest-asyncio==1.4.0
